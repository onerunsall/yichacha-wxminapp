<view class="header">
  <view class="top searchIs2_0Box">
    <view class="input_box">
      <view class="lf  input_box_view" style="display: inline-block">
        <input  focus='false'  bindinput='inputUser' placeholder-class="place_holder" class="input input_dex" value='{{inputVal}}' bindconfirm='inputUserSearch' type="text" confirm-type='search' placeholder="请输入产品名称、注册证编号等"></input>
      </view>
      <image bindtap='searchIcon' class="search_fdj rt" src="../icon/search@2x.png" alt=""></image>
      <image wx:if='{{showDel==1}}' class='del' bindtap='delThis' src='../img/close@2x.png'></image>
    <text class='refuse' bindtap='refuse'>取消</text>
    </view>
    
  </view>
  <!--热门搜索-->
  <view class="clear"></view>
  <view class="lf hot_search" wx:if="{{searchVal==1}}">
    <view class="hot_search_title">
      <text>热门搜索</text>
    </view>
    <view style="width: 100%;background-color: #ffffff;">
      <view id="textList">
        <view class="hot_search_text">
        <text bindtap="searchSpan" data-kw='{{item.keyword}}' style='background:{{item.bgColor}};color:{{item.color}}' wx:for="{{searchlist}}" wx:key="searchlist" data-index="{{index}}" data-text="{{item.hotKeyName}}">{{item.keyword}}</text>
          <!-- <view bindtap="searchSpan" data-kw='{{item.keyword}}' wx:for="{{searchlist}}" wx:key="searchlist" data-index="{{index}}" data-text="{{item.hotKeyName}}">
          <image wx:if='{{item.paixuid==1}}' src='../img/top1@2x.png'></image>
          <image wx:if='{{item.paixuid==2}}' src='../img/top2@2x.png'></image>
          <image wx:if='{{item.paixuid==3}}' src='../img/top3@2x.png'></image>
          <image class='up' wx:if='{{item.paixuid==4}}' src='../img/up@2x.png'></image>
            <text>{{item.keyword}}</text>
          </view> -->
        </view>
      </view>
    </view>
  </view>
</view>

 <!-- <view class="search">
    <form bindreset="res" bindsubmit="search">      
      <view class="df search_arr">
        <icon class="searchcion" size='20' type='search' value="{{search}}" bindtap='Search'></icon>
        <input class="searchdo" bindconfirm="search" placeholder="你想要的都在这里" bindinput="input"/>
      </view>
    </form>  
   </view> -->
  <view wx:if="{{searchVal==2}}" style='width:750rpx;background:#fff;height:96rpx;position:fixed;top:80rpx;'>
     <view class="navbar" >
        <view wx:for="{{navbar}}" data-idx="{{index}}" class="item {{currentTab==index ? 'active' : ''}}" wx:key="unique" bindtap="navbarTap">{{item}}</view>

      </view>
  </view>

<view style='margin-top:178rpx;' wx:if="{{searchVal==2}}">
  <view wx:for="{{list}}" wx:key="list" data-index="{{index}}">
       <navigator url='../detail/detail?id={{item.productId}}'>
        <view wx:if="{{productIs==0}}" style="background:#fff" class="productList_list_pic bottom lf">
          <view class="productList_list_pic lf">
            <view class='csTitle'>
              <view>
<block wx:for="{{item.name}}" wx:for-item="key_obj" wx:key="">
          <text wx:if="{{key_obj.key == true}}" style="color:rgb(2,175,101);">{{key_obj.str}}</text>
          <text wx:else>{{key_obj.str}}</text>
      </block>
                <view wx:if='{{item.coVipIs==1}}'>
                  <image src='../img/V@2x.png'></image>
                  <text>认证</text>
                </view>
                 
              </view>
            </view>
            <view class='register register1'>【注册证编号】<text >{{item.registerNo}}</text></view>
              <view class='register register2'>【注册证名称】<text >{{item.coName}}</text></view>
            <!-- <view name="productList_list_pic_ps" class="productList_list_pic_ps" id="jq1">
            <view  style='background:url({{item.pic1}}) no-repeat;background-size:cover;background-position: center;'  class="dis1 img1 color_c"></view>
              <view style='background:url({{item.pic2}}) no-repeat;background-size:cover;background-position: center;'  class="dis2 img2 color_c"></view>
              <view style='background:url({{item.pic3}}) no-repeat;background-size:cover;background-position: center;'  class="dis3 img3 color_c"></view>
            </view> -->
          </view>
        </view>
      </navigator>

         <navigator url='../vendorInformation/vendorInformation?id={{item.coId}}'>
        <view wx:if="{{productIs!=0}}" style="background:#fff;position:relative;"  class="productList_list_text lf">
          <view class='csTitleIs' wx:if='{{item.vipIs==1}}'>
              <view>
<block wx:for="{{item.name}}" wx:for-item="key_obj" wx:key="">
          <text wx:if="{{key_obj.key == true}}" style="color:rgb(2,175,101);">{{key_obj.str}}</text>
          <text wx:else>{{key_obj.str}}</text>
      </block>
              </view>
               <view>
                  <image src='../img/V@2x.png'></image>
                  <text>认证</text>
                </view>
            </view>

<view class='csTitleIsNo' wx:if='{{item.vipIs!=1}}' >
              <view >
<block wx:for="{{item.name}}" wx:for-item="key_obj" wx:key="">
          <text wx:if="{{key_obj.key == true}}" style="color:rgb(2,175,101);">{{key_obj.str}}</text>
          <text wx:else>{{key_obj.str}}</text>
      </block>
              </view>
            </view>


          <view class="productList_list_text_lf rightIcon lf" wx:if='{{item.vipIs==1}}'>
            <image src='../img/right@2x.png'></image>
          </view>

          <view class="productList_list_text_lf rightIcons lf" wx:if='{{item.vipIs!=1}}'>
            <image src='../img/right@2x.png'></image>
          </view>
        </view>
      </navigator>
  </view>
  <view class='upMore'>——  {{learnMore}} ——</view>
</view>